# Agent Foundry - Default Package List for VM Templates
#
# This file lists packages to install in Arch Linux VMs beyond the base set.
# Add one package per line. Lines starting with # are comments.
#
# Users can customize by creating ~/.config/foundry/packages.txt
#
# Base packages (always installed):
#   base, base-devel, linux, linux-firmware, git, openssh, vim, nano,
#   bash, tmux, screen, jq, curl, wget, nodejs, npm, python, python-pip, docker
#
# Add your custom packages below:

# Example: Database tools
# postgresql
# redis

# Example: Cloud CLIs
# aws-cli

# Example: Language tools
# go
# rustup

# Example: Useful CLI tools
# ripgrep
# fd
# bat
# htop
# ncdu
