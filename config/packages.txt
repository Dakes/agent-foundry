# Agent Foundry - Extra Package List for the Agent Image
#
# Packages installed into the sandbox image beyond the base set. Add one
# package per line; lines starting with # are comments. Edit this file in the
# source tree, then rebuild: foundry image build [variant]
#
# Base packages (always installed, see docker/foundry-agent.Dockerfile):
#   build-essential, ca-certificates, curl, file, git, gnupg, jq, less,
#   openssh-client, patch, python3, python3-pip, python3-venv, ripgrep, tmux,
#   unzip, vim, wget, xz-utils, nodejs, gh, sudo, docker (engine + compose)
#
# Add your custom packages below:

# Example: Database tools
# postgresql
# redis

# Example: Cloud CLIs
# aws-cli

# Example: Language tools
# go
# rustup

# Example: Useful CLI tools
# ripgrep
# fd
# bat
# htop
# ncdu
