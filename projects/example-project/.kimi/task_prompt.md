# Kimi Task Prompt

This file is used as the task prompt when running `kimi-ralph` autonomously.
The GitHub watcher overwrites this file for each triggered task.

When starting `kimi-ralph` with `foundry up` (`.agent: "kimi-ralph"`),
place the task description here first.
