# Fix Plan

1. Review `git-config.json` to understand the repositories and deploy key requirements.
2. Consult `overview.md` and `getting-started.md` for project goals and workflow expectations.
3. Inspect the cloned repositories under `repos/` and ensure they match the latest branches defined in the configuration.
4. Propose changes that keep deploy keys scoped per repo and leverage the existing `.ralph/` prompt when crafting responses.
